# Vendored sources: not ours to reformat.
^thirdparty/

# try_compile fixture: this file is fed to the compiler verbatim as a feature
# probe, so its formatting must stay byte-identical to what the probe expects.
^cmake/check-std-expected\.cpp$

# ABI layout probe: compiled with -fdump-record-layouts-complete to produce the
# class-shape baseline. Its one-line member definitions are what make the dump
# readable; a reformat would rewrite every offset line in the baseline for no
# change in the ABI.
^ci/abi/vtable-probe\.cpp$
